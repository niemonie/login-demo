import { makeStyles } from "@material-ui/core/styles";

const useSignUpFormStyles = makeStyles((theme) => ({
  form: {
    width: "100%",
    marginTop: theme.spacing(1),
  },
}));

export { useSignUpFormStyles };
